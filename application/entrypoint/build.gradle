description = "Camada de entrada de dados na aplicação(REST, AMQP, etc)"

dependencies {
    implementation project(":application:core")

    implementation libs.rest
    testImplementation libs.unit_test
}